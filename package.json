{
    "name": "parcel-sandbox",
    "version": "1.0.0",
    "description": "Platformer",
    "scripts": {
        "start": "npm run dev & npm run serve",
        "dev": "parcel index.html --open",
        "serve": "http-server -p 40 --cors",
        "build": "parcel build index.html"
    },
    "dependencies": {
        "lodash.clonedeep": "^4.5.0"
    },
    "devDependencies": {
        "@types/lodash.clonedeep": "^4.5.7",
        "parcel": "^2.6.2",
        "typescript": "^4.6.3",
        "http-server": "14.1.0"
    },
    "prettier": {
        "tabWidth": 4
    }
}
