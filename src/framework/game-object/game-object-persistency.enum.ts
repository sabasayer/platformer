export enum EnumGameObjectPersistency {
    /**
     * It will respawn with all initial values on level start
     */
    PersistentOnLevel = 1,
    /**
     * It will respawn with initial position on level
     */
    PersistentOnGame = 2,
}
